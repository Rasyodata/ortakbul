# Web ortam değişkenleri — kopyala: cp .env.example .env.local
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
# Gizli admin yolu (backend ADMIN_PATH ile aynı olmalı)
NEXT_PUBLIC_ADMIN_PATH="y-panel-8f3a"
